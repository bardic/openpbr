{
  "format_version": "1.16.100",
  "minecraft:texture_set": {
    "color": "{{ .Colour }}",
    "metalness_emissive_roughness": {{ if eq .MerType 0 }} "{{.MerFile}}" {{ else }} {{ .MerArr }} {{ end }},
    "normal": "{{ .Height }}"
  }
}
